<%= render 'search_form' %>

<ul class='cloud-tags unstyled'>
  <% @popular_gems.each do |g| %>
    <li class='size-<%= @original_gems.index(g) / (GemStats::Gem::MAX_WEIGHT) %>'><%= link_to g.name, gem_path(g) %></li>
  <% end %>
</ul>
