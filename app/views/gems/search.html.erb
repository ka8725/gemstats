<%= render 'search_form' %>

<% if @gems.present? %>
 <div class='well'>
  <h6 class='page-header'>Results for search query</h6>
  <table class='table table-striped'>
    <thead>
      <tr>
        <th>Name</th>
        <th>Downloads</th>
        <th>Version</th>
        <th>Description</th>
      </tr>
    </thead>
    <tbody>
      <% @gems.each do |g| %>
        <tr>
          <td><%= link_to g.name, gem_path(g) %></td>
          <td><%= number_with_delimiter(g.downloads, :delimiter => ' ') %></td>
          <td><%= g.version %></td>
          <td><%= g.info %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
 </div>
<% else %>
  <h2>No results found</h2>
<% end %>
